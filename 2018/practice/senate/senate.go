package senate

import (
	"io"
	"strings"
)

// Evac takes in a bunch of test cases following the
// stipulated format, solves each one, and concatenates the results
func Evac(r io.Reader) string {
	var sb strings.Builder
	return sb.String()
}

// evacuate takes in a list of parties and returns
// a list of the evacuation steps to take
func evacuate(parties []int) []string {
	plan := []string{}
	//
	return plan
}
